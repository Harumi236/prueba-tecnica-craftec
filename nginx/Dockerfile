# Use the official Nginx image
FROM nginx:alpine

# Copy the local index.html to override the default
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80

# Use the official Nginx image
FROM nginx:latest
